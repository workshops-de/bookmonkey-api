{
  "name": "bookmonkey-api",
  "description": "A simple backend for the BookMonkey example application.",
  "version": "2.0.0",
  "dependencies": {
    "json-server": "^0.8.17"
  },
  "bin": "server.js",
  "scripts": {
    "start": "node server"
  }
}
